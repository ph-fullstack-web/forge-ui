import React from 'react';
import {ThemeIcon as MantineThemeIcon, ThemeIconProps} from '@mantine/core';

export const ThemeIcon = (props: ThemeIconProps) => {
  return <MantineThemeIcon {...props} />;
};
